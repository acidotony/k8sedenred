apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ftpservicestest
  namespace: dev
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile
  volumeName: pvdev01
  resources:
    requests:
      storage: 1Gi