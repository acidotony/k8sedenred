apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fumon
  namespace: dev
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile
  volumeName: earst01dev-pvdev01
  resources:
    requests:
      storage: 1Gi